{
  "private": true,
  "scripts": {
    "test": "mocha tests/bootstrap.js --recursive tests --no-timeouts"
  },
  "devDependencies": {
    "chai": "^4.1.2",
    "mocha": "^5.2.0",
    "puppeteer": "^1.5.0",
    "random-words": "^1.1.0"
  }
}
